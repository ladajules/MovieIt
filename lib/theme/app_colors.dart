import 'package:flutter/material.dart';

class AppColors {
    AppColors._();

    static const black = Color(0xFF000000);
    static const softPeriwinkle = Color(0xFFA970FF); // primary purpol
    static const wisteria = Color(0xFFC3B1E1); // softer purpol
    static const charcoal = Color(0xFF535353);  
    static const lavender = Color(0xFFE9EBF8); 
    static const cardBody = Color(0xFF1E1E1E);
    static const white = Color(0xFFFFFFFF);

    static const plannerBg = Color(0xFF080810);
    static const plannerSurface = Color(0xFF111120);
    static const plannerCard = Color(0xFF13131F);
    static const cardBorder = Color(0xFF1E1E32);
    static const headerBg = Color(0xFF1A1A2E);
    
    static const accentDim = Color(0xFF7B3FBF);
    static const accentSoft = Color(0xFF2D1A4A);
    static const successGreen = Color(0xFF4ADE80);
    static const successGreenDim = Color(0xFF1A3A2A);
    
    static const textSecondary = Color(0xFFB0B0C8); 
    static const textMuted = Color(0xFF6A6A8A); 
    static const dividerSubtle = Color(0xFF252535);
}